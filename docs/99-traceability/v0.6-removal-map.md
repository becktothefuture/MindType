<!--══════════════════════════════════════════════════════════
  ╔══════════════════════════════════════════════════════════════╗
  ║  ░  V 0 . 6   R E M O V A L   &   R E W R I T E   M A P  ░░░  ║
  ║                                                              ║
  ║                                                              ║
  ║                                                              ║
  ║                                                              ║
  ║           ╌╌  P L A C E H O L D E R  ╌╌                      ║
  ║                                                              ║
  ║                                                              ║
  ║                                                              ║
  ║                                                              ║
  ╚══════════════════════════════════════════════════════════════╝
    • WHAT ▸ Mapping of files to delete/keep/rewrite for v0.6
    • WHY  ▸ Ensure clean LM‑only pipeline; externalize undo
    • HOW  ▸ Phase 1 removes rules; later phases rewrite LM paths
-->

# v0.6 Removal & Rewrite Map

| Path | Pre‑v0.6 role | Action | Rationale | References |
|---|---|---|---|---|
| `engines/noiseTransformer.ts` | Rule‑based tidy sweep | Remove rule logic; keep stub (returns null) until P2 LM rewrite | LM‑only transformers (PRD v0.6); keep module to avoid widespread import churn | `core/sweepScheduler.ts`, `core/diffusionController.ts`, tests `sweep*`, `noiseTransformer*` |
| `core/undoIsolation.ts` | System edit grouping/rollback | Delete | Undo is external; rollback handled via Cmd+Alt+Z wave history in P4 | tests `undoIsolation.spec.ts` |
| `ui/groupUndo.ts` | Group diffs per sweep | Delete | Same as above | tests `groupUndo.spec.ts` |
| `core/api/denoise.ts` | Batch denoise adapter | Delete | Out of scope; LM wave only; doc removed | tests `denoise.spec.ts` |
| Docs `add-a-grammar-rule.md` | Rule how‑to | Deleted | Rules removed | — |
| Docs `denoise-api.md` | Denoise API | Deleted | Not in v0.6 | — |
| `web-demo` (legacy) | Old demo app | N/A (already `playground/`) | v0.6 uses single Playground | — |

Notes

- Noise transformer LM rewrite lands in P2 at the same path with LM‑only behavior within the Active Region.
- Tests directly validating rules are removed; scheduler/integration tests remain and mock the stub as needed.




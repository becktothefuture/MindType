/*  ║▓▒░  G R O U P   U N D O  ░▒▓║
WHAT ▸ Merge AI diffs into a single undo step per sweep */

export function groupUndo<T>(_diffs: T[]): T[] {
    /* ⟢ TODO: group by sweep id and time window */
    return _diffs;
  }